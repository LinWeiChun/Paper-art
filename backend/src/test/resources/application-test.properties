spring.datasource.url=jdbc:h2:mem:paperart;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=YEAR
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

cloudflare.r2.access-key=test-access-key
cloudflare.r2.secret-key=test-secret-key
cloudflare.r2.bucket=test-bucket
cloudflare.r2.endpoint=http://localhost:9000
cloudflare.r2.public-url=http://localhost:9000/test-bucket

jwt.secret=paperart-test-secret-that-is-at-least-thirty-two-bytes
jwt.cookie-secure=false
csrf.cookie-secure=false

admin.username=test-admin
admin.password=test-password

app.cors.allowed-origins=http://localhost:5173
